diff -u old//sample1.4.tex new//sample1.4.tex
--- old//sample1.4.tex	2011-12-28 23:56:00.000000000 +0400
+++ new//sample1.4.tex	2011-12-28 23:58:06.077567368 +0400
@@ -81,7 +81,53 @@
 \setcounter{sectocnonumdepth}{3}
 \tableofcontents
 
+
 \newpage
+\section{Задание}
+Задан язык:
+\begin{equation}\label{eq:s0b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a b}\right\}^k}\right\}^2} \cdot {\left\{{\left\{{b c}\right\}^m}\right\}^2}}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}
+\end{split}
+\end{equation}
+\section{Этап 1}
+Рассмотрим, какие цепочки принадлежат искомому множеству \emph{L}.
+
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a b}\right\}^k}\right\}^2} \cdot {\left\{{\left\{{b c}\right\}^m}\right\}^2}}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&= \left\{{{\left\{{{a b, (a b)^2, (a b)^3,\dots}}\right\}^2} \cdot {\left\{{{\varepsilon ,bc, (bc)^2, (bc)^3,\dots}}\right\}^2}} \right\}= \\
+&= \left\{{{\left\{{{a b \cdot ab , (a b)^2 \cdot ab,\dots, ab \cdot (ab)^2, (ab)^2 \cdot (ab)^2,\dots}}\right\}} \cdot {\left\{{{\varepsilon \cdot \varepsilon , bc \cdot \varepsilon , (bc)^2 \cdot \varepsilon ,\dots, \varepsilon \cdot bc, bc \cdot bc,\dots}}\right\}}} \right\}= \\
+&= \left\{{{\left\{{{(ab)^2, (ab)^3, (ab)^4,\dots}}\right\}} \cdot {\left\{{{\varepsilon ,bc, (bc)^2, (bc)^3,\dots}}\right\}}} \right\}
+\end{split}
+\end{equation}
+
+Проверим, к какому типу, по классификации Хомского, относится данный язык. Для этого воспользуемся леммой о разрастании регулярных языков.
+Возьмем достаточно длинную цепочку языка: 
+\begin{math}
+\alpha=abababbcbc
+\end{math}
+согласно лемме, необходимо найти хотя бы одну подцепочку, для которой бы выполнялись её условия. Представим исходную цепочку в виде 
+\begin{math}
+\alpha=\gamma\beta\delta
+\end{math}
+где \begin{math}\beta=ab, \gamma=abab, \delta=bcbc\end{math}. Теперь выполним повторения для выделенной подцепочки
+\begin{math}\beta\end{math}. В итоге получаем
+
+для \begin{math}i=0\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^0\delta=ababbcbc \in L\end{math},
+
+для \begin{math}i=1\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^1\delta=abababbcbc \in L\end{math},
+
+для \begin{math}i=2\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^2\delta=ababababbcbc \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+для \begin{math}i=k\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^k\delta=\gamma{\underbrace{\beta\dots\beta}_\text{k раз}}\delta=abab(ab)^kbcbc \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+Заметим, что существует такая цепочка, для которой выполняются условия леммы, что говорит о том, что данный язык является регулярным, и мы можем применить алгоритм построения регулярной грамматики по регулярному выражению.
+
 \section{Этап 2}
 \subsection{Этап 2.1}
 Перед построением регулярной грамматики для регулярного
@@ -5460,7 +5506,7 @@
 
 \begin{math}
 {S_{8}}=
-abab{{(ab)}^{*}}(\varepsilon  + bc{{(bc)}^{*}})\end{math}.
+abab{{(ab)}^{*}}(\varepsilon  + bc{{(bc)}^{*}})=ab{{(ab)}^{+}}{{(bc)}^{*}}\end{math}.
 \subsection{Этап 2.3 (правосторонняя)}
 Построим праволинейную грамматику для выражения (\ref{eq:s2b2}). Воспользуемся рекурсивным определением регулярного выражения 
 для построения последовательности грамматик каждого 
@@ -10905,5 +10951,35 @@
 
 \begin{math}
 {S_{2}}=
-abab{{(ab)}^{*}}(\varepsilon  + bc{{(bc)}^{*}})\end{math}.
+abab{{(ab)}^{*}}(\varepsilon  + bc{{(bc)}^{*}})=ab{{(ab)}^{+}}{{(bc)}^{*}}\end{math}.
+
+\subsubsection{Этап 2.10.3}
+Так как регулярные выражения для леволинейной и праволинейной грамматик совпали далее не будем подразделять шаги.
+Для полученного регулярного выражения построим регулярное множество, которое примет вид
+\begin{equation}\label{eq:s1023b1}
+\begin{split}
+L' &= \left\{{\{a b\}\cdot{\left\{a b\right\}^k}} \cdot {\left\{b c\right\}^m}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}
+\end{split}
+\end{equation}
+
+\subsubsection{Этап 2.10.4}
+Сравним полученное множество (\ref{eq:s1023b1}) с исходным (\ref{eq:s0b1}). Для этого рассмотрим множество цепочек исходного множества:
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a b}\right\}^k}\right\}^2} \cdot {\left\{{\left\{{b c}\right\}^m}\right\}^2}}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&= \left\{{{\left\{{{a b, (a b)^2, (a b)^3,\dots}}\right\}^2} \cdot {\left\{{{\varepsilon ,bc, (bc)^2, (bc)^3,\dots}}\right\}^2}} \right\}= \\
+&= \left\{{{\left\{{{a b \cdot ab , (a b)^2 \cdot ab,\dots, ab \cdot (ab)^2, (ab)^2 \cdot (ab)^2,\dots}}\right\}} \cdot {\left\{{{\varepsilon \cdot \varepsilon , bc \cdot \varepsilon , (bc)^2 \cdot \varepsilon ,\dots, \varepsilon \cdot bc, bc \cdot bc,\dots}}\right\}}} \right\}= \\
+&= \left\{{{\left\{{{(ab)^2, (ab)^3, (ab)^4,\dots}}\right\}} \cdot {\left\{{{\varepsilon ,bc, (bc)^2, (bc)^3,\dots}}\right\}}} \right\}
+\end{split}
+\end{equation}
+
+Теперь рассмотрим цепочки, входящие в полученное множество:
+\begin{equation}
+\begin{split}
+L' &= \left\{{\{a b\}\cdot{\left\{a b\right\}^k}} \cdot {\left\{b c\right\}^m}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+ &= \left\{{{\left\{{{(ab)^2, (ab)^3, (ab)^4,\dots}}\right\}} \cdot {\left\{{{\varepsilon ,bc, (bc)^2, (bc)^3,\dots}}\right\}}} \right\}
+\end{split}
+\end{equation}
+
+Видно, что полученные множества совпадают, а значит языки \begin{math}L\end{math} и \begin{math}L'\end{math} эквивалентны.
 \end{document}
