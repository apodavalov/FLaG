--- a/sample3.16.tex	2012-12-25 19:08:19.000000000 -0800
+++ b/sample3.16.tex	2012-12-25 19:15:37.307982687 -0800
@@ -83,6 +83,49 @@
 \tableofcontents
 
 \newpage
+\section{Задание}
+Задан язык:
+\begin{equation}\label{eq:s0b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a,b}\right\}^2}\right\}^k},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+
+\section{Этап 1}
+Рассмотрим, какие цепочки принадлежат искомому множеству \emph{L}.
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a,b}\right\}^2}\right\}^k},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k>0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{ \left\{ a^2, a^4, \dots, b^2, b^4, \dots, ab, abaa, abab \dots, abba, \dots \right\}, \left\{ \varepsilon, (ba)^2, (ba)^4 \dots \right\} \right\}= \\
+&=\left\{ \left\{ (a^lb^m)^{2k} \right\} \mid \forall k \geq 0,l \geq 0,m \geq 0, \text{где } k,l,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+
+Проверим, к какому типу языков, по классификации Хомского, относится данный язык. Для этого воспользуемся леммой о разрастании
+регулярных языков.
+
+Итак, возьмем достаточно длинную цепочку символов, принадлежащую языку \begin{math}L\end{math}
+\begin{math}
+\alpha=(a^{k_1}c^{k_2})^{k_3}
+\end{math}, где \begin{math}k_1,k_2,k_3\end{math} --- константы (Например, цепочку \begin{math}\alpha=ababababbb\end{math}).
+Согласно лемме о разрастании регулярных языков требуется найти хотя бы одну подцепочку, для которой выполнялись бы условия леммы.
+Представим исходную цепочку \begin{math}\alpha\end{math} в виде \begin{math}\alpha=\gamma\beta\delta\end{math}, где 
+\begin{math}\beta=abab, \gamma=abab, \delta=bb\end{math}. Теперь выполним повторения для выделенной подцепочки
+\begin{math}\beta\end{math}. В итоге получаем
+
+для \begin{math}i=0\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^0\delta=\gamma\delta=ababbb=(ab)^2b^2 \in L\end{math},
+
+для \begin{math}i=1\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^1\delta=\gamma\delta=ababababbb=(ab)^4b^2 \in L\end{math},
+
+для \begin{math}i=2\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^2\delta=\gamma\delta=abab(abab)^2bb=(ab)^6b^2 \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+для \begin{math}i=k\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^k\delta=\gamma{\underbrace{\beta\dots\beta}_\text{k раз}}\delta=abab(abab)^kbb=(ab)^{2k+2}b^2 \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+Заметим, что существует такая цепочка, принадлежащая исходному языку, для которой выполняются условия леммы, а это
+значит, что данный язык \begin{math}L\end{math} является регулярным.
+
 \section{Этап 2}
 \subsection{Этап 2.1}
 Перед построением регулярной грамматики для регулярного
@@ -8806,7 +8849,7 @@
 
 \begin{math}
 {S_{6}}=
-{{(a + b)}^{2}}{{({{(a + b)}^{2}})}^{*}}\end{math}.
+{{(a + b)}^{2}}{{({{(a + b)}^{2}})}^{*}}={{({{(a + b)}^{2}})}^{+}}\end{math}.
 \subsubsection{Этап 2.11.3 (левосторонняя)}
 Используя теорию уравнений с регулярными коэффициентами, выполним построение
 регулярного выражения для автомата
@@ -17578,7 +17621,7 @@
 
 \begin{math}
 {S_{2}}=
-{{(a + b)}^{2}}{{({{(a + b)}^{2}})}^{*}}\end{math}.
+{{(a + b)}^{2}}{{({{(a + b)}^{2}})}^{*}}={{({{(a + b)}^{2}})}^{+}}\end{math}.
 \subsubsection{Этап 2.11.3 (правосторонняя)}
 Используя теорию уравнений с регулярными коэффициентами, выполним построение
 регулярного выражения для автомата
@@ -17646,4 +17689,24 @@
 \begin{math}
 {S_{5}}=
 {{({{(a + b)}^{2}})}^{*}}\end{math}.
+\subsubsection{Этап 2.11.4}
+Так как регулярные выражения для леволинейной и праволинейной грамматик попарно совпали далее не будем подразделять шаги.
+Так как исходное регулярное выражение допускает пустую цепочку, добавим возможность вывода пустой цепочки к выражению, полученному
+преобразованием конечного автомата в грамматику, а затем в регулярное выражение.
+
+\begin{math}{{({{(a + b)}^{2}})}^{+}} + \varepsilon={{({{(a + b)}^{2}})}^{*}}\end{math}
+
+Видим, что полученное множество совпадает с множеством, полученным преобразованием конечного автомата в регулярное выражение.
+Таким образом, все полученные множества совпали между собой.
+
+Для полученного регулярного выражения построим регулярное множество, которое примет вид
+\begin{equation}\label{eq:s1023b1}
+\begin{split}
+L' &= \left\{{{\left\{{\left\{{a,b}\right\}^2}\right\}^k}}\mid  \forall k \geq 0, \text{где } k  \text{ --- целое}\right\}= \\
+&=\left\{ \left\{ (a^lb^m)^{2k} \right\} \mid \forall k \geq 0,l \geq 0,m \geq 0, \text{где } k,l,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+
+\subsubsection{Этап 2.11.5}
+Сравним полученное множество (\ref{eq:s1023b1}) с исходным (\ref{eq:s0b1}). Видим, что языкам принадлежит одно и то же множество цепочек.
+Языки эквивалентны.
 \end{document}
