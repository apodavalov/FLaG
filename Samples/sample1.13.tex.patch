--- a/sample1.13.tex	2012-12-25 16:47:05.000000000 -0800
+++ b/sample1.13.tex	2012-12-25 17:16:49.771783882 -0800
@@ -83,6 +83,47 @@
 \tableofcontents
 
 \newpage
+\section{Задание}
+Задан язык:
+\begin{equation}\label{eq:s0b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a b}\right\}^2}\right\}^k} \cdot {\left\{{\left\{{b c}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+\section{Этап 1}
+Рассмотрим, какие цепочки принадлежат искомому множеству \emph{L}.
+
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a b}\right\}^2}\right\}^k} \cdot {\left\{{\left\{{b c}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&= \left\{{\left\{{{\varepsilon , (ab)^2, (ab)^4, \dots}}\right\}} \cdot {\left\{{{\varepsilon ,(bc)^2, (bc)^4,\dots}}\right\}} \right\}\end{split}
+\end{equation}
+
+Проверим, к какому типу, по классификации Хомского, относится данный язык. Для этого воспользуемся леммой о разрастании регулярных языков.
+Возьмем достаточно длинную цепочку языка: 
+\begin{math}
+\alpha=ababababbcbc
+\end{math}
+согласно лемме, необходимо найти хотя бы одну подцепочку, для которой бы выполнялись её условия. Представим исходную цепочку в виде 
+\begin{math}
+\alpha=\gamma\beta\delta
+\end{math}
+где \begin{math}\beta=abab, \gamma=abab, \delta=bcbc\end{math}. Теперь выполним повторения для выделенной подцепочки
+\begin{math}\beta\end{math}. В итоге получаем
+
+для \begin{math}i=0\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^0\delta=ababbcbc \in L\end{math},
+
+для \begin{math}i=1\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^1\delta=ababababbcbc \in L\end{math},
+
+для \begin{math}i=2\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^2\delta=abab(abab)^2bcbc \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+для \begin{math}i=k\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^k\delta=\gamma{\underbrace{\beta\dots\beta}_\text{k раз}}\delta=(ab)^{2k+2}bcbc \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+Заметим, что существует такая цепочка, для которой выполняются условия леммы, что говорит о том, что данный язык является регулярным, и мы можем применить алгоритм построения регулярной грамматики по регулярному выражению.
+
 \section{Этап 2}
 \subsection{Этап 2.1}
 Перед построением регулярной грамматики для регулярного
@@ -8927,7 +8968,7 @@
 
 \begin{math}
 {S_{10}}=
-bcbc{{(bcbc)}^{*}} + abab{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})\end{math}.
+bcbc{{(bcbc)}^{*}} + abab{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})={({(ab)^2})^{+}}{({(bc)^2})^{*}} + {({(bc)^2})^{+}}\end{math}.
 \subsubsection{Этап 2.11.3 (левосторонняя)}
 Используя теорию уравнений с регулярными коэффициентами, выполним построение
 регулярного выражения для автомата
@@ -9217,7 +9258,7 @@
 
 \begin{math}
 {S_{10}}=
-{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})\end{math}.
+{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})={({(ab)^2})^{*}}{({(bc)^2})^{*}}\end{math}.
 \subsection{Этап 2.3 (правосторонняя)}
 \subsubsection{Этап 2.3.1 (правосторонняя)}
 Построим праволинейную грамматику для выражения (\ref{eq:s2b2}). Воспользуемся рекурсивным определением регулярного выражения 
@@ -17927,7 +17968,7 @@
 
 \begin{math}
 {S_{7}}=
-bcbc{{(bcbc)}^{*}} + abab{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})\end{math}.
+bcbc{{(bcbc)}^{*}} + abab{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})={({(ab)^2})^{+}}{({(bc)^2})^{*}} + {({(bc)^2})^{+}}\end{math}.
 \subsubsection{Этап 2.11.3 (правосторонняя)}
 Используя теорию уравнений с регулярными коэффициентами, выполним построение
 регулярного выражения для автомата
@@ -18130,5 +18171,28 @@
 
 \begin{math}
 {S_{5}}=
-{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})\end{math}.
+{{(abab)}^{*}}(\varepsilon  + bcbc{{(bcbc)}^{*}})={({(ab)^2})^{*}}{({(bc)^2})^{*}}\end{math}.
+\subsubsection{Этап 2.11.4}
+Так как регулярные выражения для леволинейной и праволинейной грамматик попарно совпали далее не будем подразделять шаги.
+Множество, полученное преобразованием конечного автомата является эквивалентным исходному.
+Множество, полученное преобразованием конечного автомата в грамматику, а затем в регулярное выражение описывает множество 
+почти эквивалентное исходному. Разница лишь в том, что
+множеству описанному данным регулярным выражением не принадлежит пустая цепочка, в отличие от множества, заданного
+исходным регулярным выражением. Докажим это, добавив возможность вывода пустой цепочки к полученному регулярному выражению.
+
+\begin{math}{({(ab)^2})^{+}}{({(bc)^2})^{*}} + {({(bc)^2})^{+}} + \varepsilon = {({(ab)^2})^{+}}{({(bc)^2})^{*}} + {({(bc)^2})^{*}}=({({(ab)^2})^{+}} + \varepsilon){({(bc)^2})^{*}}={({(ab)^2})^{*}}{({(bc)^2})^{*}}\end{math}
+
+Видим, что полученное множество совпадает с множеством, полученным преобразованием конечного автомата в регулярное выражение.
+Таким образом, все множества совпали с исходным регулярным выражением.
+
+Для полученного регулярного выражения построим регулярное множество, которое примет вид
+\begin{equation}\label{eq:s1023b1}
+\begin{split}
+L' &= \left\{{{\left\{{\left\{{a b}\right\}^2}\right\}^k} \cdot {\left\{{\left\{{b c}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{{{\left\{{\left\{{a b}\right\}^2}\right\}^k} \cdot {\left\{{\left\{{b c}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m \geq 0, \text{где } k,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+
+\subsubsection{Этап 2.11.5}
+Сравним полученное множество (\ref{eq:s1023b1}) с исходным (\ref{eq:s0b1}). Языки задаются абсолютно одинаково.
+Рассматривать цепочки смысла не имеет. Языки эквивалентны.
 \end{document}
