diff -u old//sample3.6.tex new//sample3.6.tex
--- old//sample3.6.tex	2011-12-26 20:19:24.000000000 +0400
+++ new//sample3.6.tex	2011-12-26 20:17:40.000000000 +0400
@@ -82,6 +82,51 @@
 \tableofcontents
 
 \newpage
+\section{Задание}
+Задан язык:
+\begin{equation}\label{eq:s0b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a,b}\right\}^k}\right\}^2},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m>0, \text{где } k,m  \text{ --- целые}\right\}\end{split}
+\end{equation}
+
+\section{Этап 1}
+Рассмотрим, какие цепочки принадлежат искомому множеству \emph{L}.
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a,b}\right\}^k}\right\}^2},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m>0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{{{\left\{{\left\{{a,b}\right\}^k}\right\}^2},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m>0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{ \left\{ \varepsilon, a, a^2, \dots, b, b^2, \dots, ab, (ab)^2, \dots, ab^2, ab^3, ab^4, \dots \right\}^2 , \left\{ (ba)^2, (ba)^4, (ba)^6, \dots \right\} \right\}= \\
+&=\left\{ \left\{ \varepsilon, a, a^2, \dots, b, b^2, \dots, ab, (ab)^2, \dots, ab^2, ab^3, ab^4, \dots \right\} , \left\{ (ba)^2, (ba)^4, (ba)^6, \dots \right\} \right\}= \\
+&=\left\{ \left\{ (a^kb^l)^m \right\} \mid \forall k \geq 0,l \geq 0, m \geq 0, \text{где } k,l,m  \text{ --- целые} \right\}\end{split}
+\end{equation}
+
+Проверим, к какому типу языков, по классификации Хомского, относится данный язык. Для этого воспользуемся леммой о разрастании
+регулярных языков.
+
+Итак, возьмем достаточно длинную цепочку символов, принадлежащую языку \begin{math}L\end{math}
+\begin{math}
+\alpha=(a^{k_1}b^{k_2})^{k_3}
+\end{math}, где \begin{math}k_1,k_2,k_3\end{math} --- константы (Например, цепочку \begin{math}\alpha=ababababbba\end{math}).
+Согласно лемме о разрастании регулярных языков требуется найти хотя бы одну подцепочку, для которой выполнялись бы условия леммы.
+Представим исходную цепочку \begin{math}\alpha\end{math} в виде \begin{math}\alpha=\gamma\beta\delta\end{math}, где 
+\begin{math}\beta=ab, \gamma=ab, \delta=ababbba\end{math}. Теперь выполним повторения для выделенной подцепочки
+\begin{math}\beta\end{math}. В итоге получаем
+
+для \begin{math}i=0\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^0\delta=\gamma\delta=abababbba=(ab)^3b^2a \in L\end{math},
+
+для \begin{math}i=1\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^1\delta=\gamma\delta=ababababbba=(ab)^4b^2a \in L\end{math},
+
+для \begin{math}i=2\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^2\delta=\gamma\delta=ab(ab)^2ababbba=(ab)^5b^2a \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+для \begin{math}i=k\end{math} цепочку вида \begin{math}\alpha'=\gamma{\beta}^k\delta=\gamma{\underbrace{\beta\dots\beta}_\text{k раз}}\delta=ab(ab)^kababbba=(ab)^{k+3}b^2a \in L\end{math},
+
+\begin{math}\dots\end{math}
+
+Заметим, что существует такая цепочка, принадлежащая исходному языку, для которой выполняются условия леммы, а это
+значит, что данный язык \begin{math}L\end{math} является регулярным.
+
 \section{Этап 2}
 \subsection{Этап 2.1}
 Перед построением регулярной грамматики для регулярного
@@ -9691,4 +9736,38 @@
 \begin{math}
 {{S_{1}}''}=
 {{(a + b)}^{+}}\end{math}.
+
+\subsubsection{Этап 2.10.3}
+Так как регулярные выражения для леволинейной и праволинейной грамматик совпали далее не будем подразделять шаги.
+Очевидно, что данное выражение описывает множество почти эквивалентное исходному. Разница лишь в том, что
+множеству описанному данным регулярным выражением не принадлежит пустая цепочка, в отличие от множества, заданного
+исходным регулярным выражением. Докажим это, добавив возможность вывода пустой цепочки к полученному регулярному выражению.
+
+\begin{math}{{(a + b)}^{+}} + \varepsilon={{(a + b)}^{*}}\end{math}
+
+Для полученного регулярного выражения построим регулярное множество, которое примет вид
+\begin{equation}\label{eq:s1023b1}
+\begin{split}
+L' &= \left\{ \left\{ a,b \right\}^k \mid \forall k \geq 0, \text{где } k \text{ --- целое}\right\}\end{split}
+\end{equation}
+
+\subsubsection{Этап 2.10.4}
+Сравним полученное множество (\ref{eq:s1023b1}) с исходным (\ref{eq:s0b1}). Для этого рассмотрим множество цепочек исходного множества:
+\begin{equation}\label{eq:s1b1}
+\begin{split}
+L &= \left\{{{\left\{{\left\{{a,b}\right\}^k}\right\}^2},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m>0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{{{\left\{{\left\{{a,b}\right\}^k}\right\}^2},{\left\{{\left\{{b a}\right\}^2}\right\}^m}}\mid  \forall k \geq 0,m>0, \text{где } k,m  \text{ --- целые}\right\}= \\
+&=\left\{ \left\{ \varepsilon, a, a^2, \dots, b, b^2, \dots, ab, (ab)^2, \dots, ab^2, ab^3, ab^4, \dots \right\}^2 , \left\{ (ba)^2, (ba)^4, (ba)^6, \dots \right\} \right\}= \\
+&=\left\{ \left\{ \varepsilon, a, a^2, \dots, b, b^2, \dots, ab, (ab)^2, \dots, ab^2, ab^3, ab^4, \dots \right\} , \left\{ (ba)^2, (ba)^4, (ba)^6, \dots \right\} \right\}= \\
+&=\left\{ \left\{ (a^kb^l)^m \right\} \mid \forall k \geq 0,l \geq 0, m \geq 0, \text{где } k,l,m  \text{ --- целые} \right\}\end{split}
+\end{equation}
+
+Теперь рассмотрим цепочки, входящие в полученное множество:
+\begin{equation}
+\begin{split}
+L' &= \left\{ \left\{ a,b \right\}^k \mid \forall k \geq 0, \text{где } k \text{ --- целое}\right\}= \\
+&=\left\{ \left\{ (a^kb^l)^m \right\} \mid \forall k \geq 0,l \geq 0, m \geq 0, \text{где } k,l,m  \text{ --- целые} \right\}\end{split}
+\end{equation}
+
+Видно, что полученные множества совпадают, а значит языки \begin{math}L\end{math} и \begin{math}L'\end{math} эквивалентны.
 \end{document}
